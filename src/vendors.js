// TODO: Require these by iterating over deps in package.json
// see react-boilerplate implementation for inspiration

// const package = require('../package.json')

// This throws a warning about critical dependencies! Boo...

// Object.keys(package.dependencies).forEach(key => {
//   require(package.dependencies[key])
// })
